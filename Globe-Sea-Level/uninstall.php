<?php
/**
 * Uninstall handler for Sea Level Globe.
 *
 * The plugin stores no options, custom tables, or transients,
 * so there is nothing to clean up. This file exists so the
 * plugin uninstalls cleanly and passes directory review.
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
